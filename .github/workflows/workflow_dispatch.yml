name: Workflow dispatch
on:
  workflow_dispatch:
    paths:
      - 'changes/**'
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - run: cat $GITHUB_EVENT_PATH
